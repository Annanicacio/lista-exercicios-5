email = input("Digite seu email: ")
email = list(email)
if "@" not in email and email[len(email)-1] != "m":
    if email[(len(email))-2] != "o" and email[len(email)-3] != "c" and email[len(email)-4] != ".":
        print("O email não é valido :/")
else:
    print("O email é válido!")
