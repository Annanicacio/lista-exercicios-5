def fatorial (n):
    contador = 1
    resultado = 1
    while contador <= n:
        resultado = contador * resultado
        contador += 1
    print('{} fatorial é: {}'.format(n, resultado))

n = int(input('Qual numero voce quer o fatorial? '))
fatorial(n)
